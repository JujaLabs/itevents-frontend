{
  "name": "it-events-bootstrap",
  "private": true,
  "version": "0.0.0",
  "description": "front end from ItEvents",
  "repository": "https://github.com/JuniorsJava/itevents-bootstrap",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "grunt": "^0.4.5",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server app -a localhost -p 8000 -c-1"
  }
}
